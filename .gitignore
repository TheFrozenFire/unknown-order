# Rocq / Coq build artifacts
*.vo
*.vok
*.vos
*.glob
*.aux
.*.aux
.lia.cache
.nia.cache
.coq-native/
CoqMakefile*
Makefile.coq*

# editor / OS
*~
.DS_Store

# rocq-mcp scratch (session-local)
rocq/rocq_mcp_cache_*.v

# print-assumptions per-theorem dumps (summary.* may be committed)
rocq/print_assumptions_snapshot/current/*.txt
rocq/print_assumptions_snapshot/baseline/*.txt

# local MCP pin (absolute paths)
.grok/
