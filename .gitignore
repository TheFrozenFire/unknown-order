# Rocq / Coq build artifacts
*.vo
*.vok
*.vos
*.glob
*.aux
.*.aux
.lia.cache
.nia.cache
.coq-native/
CoqMakefile*
Makefile.coq*

# editor / OS
*~
.DS_Store

# rocq-mcp scratch (session-local)
rocq/rocq_mcp_cache_*.v

# print-assumptions dumps (commit baseline/summary.* only)
rocq/print_assumptions_snapshot/current/
rocq/print_assumptions_snapshot/baseline/*.txt

# local MCP pin (absolute paths)
.grok/

# regenerated CAS verbose dump
cas/verbose_dump.out
